# API base URL
VITE_API_URL=https://api.example.com/api

# Base URL for public booking page (e.g. https://app.example.com).
# Used to build booking links: {base}/book/{configId}
# Falls back to window.location.origin if not set.
VITE_PUBLIC_BOOKING_BASE_URL=http://localhost:8001
