// Same component as the retailer leads page — it reads the optional `:leadId`
// segment via useParams and auto-opens the LeadDetailSheet when present.
export { meta, default } from "./db-brand.retailers.$retailerId.leads";
